services:
  - type: web
    name: reflex-tailor
    env: docker
    plan: starter
    envVars:
      - key: PORT
        value: 8000
      - key: REFLEX_ENV
        value: prod
      - key: REFLEX_LOG_LEVEL
        value: info